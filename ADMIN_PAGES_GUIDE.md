# 🎛️ دليل صفحات الأدمن الكامل

## 📋 نظرة عامة:

تم إنشاء **4 صفحات رئيسية** للوحة تحكم الأدمن مع بيانات حقيقية من قاعدة البيانات:

```
✅ /admin - الصفحة الرئيسية (Dashboard)
✅ /admin/users - إدارة المستخدمين
✅ /admin/tasks - إدارة المهام
✅ /admin/withdrawals - طلبات السحب
```

---

## 1️⃣ الصفحة الرئيسية - Dashboard

**الرابط:** `/admin`

### 📊 الإحصائيات المعروضة:

```typescript
✅ إجمالي المستخدمين (من Database)
✅ المهام النشطة (من Database)
✅ إجمالي الأرصدة (حساب من جميع المستخدمين)
✅ طلبات السحب المعلقة (من Database)
```

### 🔗 روابط سريعة:

- **المستخدمون** → `/admin/users`
- **المهام** → `/admin/tasks`
- **طلبات السحب** → `/admin/withdrawals`
- **الإشعارات** (قريباً)

### 📈 النشاط الأخير:

- تسجيل مستخدمين جدد
- إكمال المهام
- طلبات السحب الجديدة

### ⚡ إجراءات سريعة:

- إضافة مهمة جديدة
- إرسال إشعار عام
- عرض المستخدمين
- الإعدادات

---

## 2️⃣ إدارة المستخدمين

**الرابط:** `/admin/users`

### 📊 الإحصائيات:

```typescript
{
  total: إجمالي المستخدمين,
  active: المستخدمون النشطون,
  totalBalance: إجمالي الأرصدة,
  avgBalance: متوسط الرصيد
}
```

### 🔍 البحث والفلترة:

**البحث:**
- بالاسم (firstName/lastName)
- باسم المستخدم (username)
- برقم Telegram ID

**الفلاتر:**
- الحالة: `ACTIVE` | `INACTIVE` | `BANNED`
- الترتيب: الأحدث | الرصيد | الإحالات | المهام

### 📋 البيانات المعروضة لكل مستخدم:

```typescript
{
  // معلومات أساسية
  firstName: "اسم المستخدم",
  lastName: "اسم العائلة",
  username: "@username",
  telegramId: "123456789",
  
  // إحصائيات
  balance: 2000,           // ✅ من Database
  tasksCompleted: 5,       // ✅ من Database
  referralCount: 2,        // ✅ من Database
  level: "BEGINNER",       // ✅ من Database
  
  // حالة
  status: "ACTIVE",
  createdAt: "2024-01-01",
  lastActiveAt: "2024-01-15"
}
```

### ⚙️ الإجراءات المتاحة:

```
👁️ عرض التفاصيل
✏️ تعديل
🚫 حظر/إلغاء الحظر
```

### 💡 مثال على الاستخدام:

```typescript
// البحث عن مستخدم
1. اكتب username في مربع البحث
2. اختر الفلتر (مثلاً: ACTIVE)
3. رتب حسب الرصيد
4. انقر على "عرض" لرؤية التفاصيل
```

---

## 3️⃣ إدارة المهام

**الرابط:** `/admin/tasks`

### 📊 الإحصائيات:

```typescript
{
  total: إجمالي المهام,
  active: المهام النشطة,
  completed: مرات الإكمال,
  totalRewards: المكافآت الموزعة
}
```

### 🎯 البيانات المعروضة لكل مهمة:

```typescript
{
  // معلومات أساسية
  name: "متابعة قناة تليجرام",
  description: "تابع قناتنا واحصل على المكافأة",
  reward: 5000,                      // ✅ من Database
  
  // تصنيف
  difficulty: "EASY" | "MEDIUM" | "HARD",
  category: "SOCIAL_MEDIA",
  
  // حالة
  isActive: true,                    // ✅ من Database
  completionCount: 15,               // ✅ من Database
  createdAt: "2024-01-01"
}
```

### 🔍 الفلاتر:

- **المستوى:** سهل | متوسط | صعب
- **الحالة:** نشطة | غير نشطة

### ⚙️ الإجراءات المتاحة:

```
✅ تفعيل/تعطيل المهمة
👁️ عرض التفاصيل
✏️ تعديل المهمة
🗑️ حذف المهمة
➕ إضافة مهمة جديدة
```

### 🎨 التصنيفات:

```typescript
// الصعوبة
EASY   → 🟢 سهل    (أخضر)
MEDIUM → 🟡 متوسط  (أصفر)
HARD   → 🔴 صعب    (أحمر)

// الفئات
SOCIAL_MEDIA  → وسائل التواصل
TELEGRAM      → تليجرام
WEBSITE       → الموقع
REFERRAL      → الإحالات
DAILY         → يومية
```

### 🔄 تفعيل/تعطيل المهمة:

```typescript
// API Endpoint
PATCH /api/admin/tasks/[id]/toggle

// النتيجة:
- المهمة النشطة → تظهر للمستخدمين في /mini-app/tasks
- المهمة المعطلة → لا تظهر للمستخدمين
```

---

## 4️⃣ طلبات السحب

**الرابط:** `/admin/withdrawals`

### 📊 الإحصائيات:

```typescript
{
  total: إجمالي الطلبات,
  pending: قيد المراجعة,
  completed: مكتمل,
  rejected: مرفوض,
  totalAmount: إجمالي المبالغ,
  pendingAmount: المبالغ المعلقة
}
```

### 📋 البيانات المعروضة لكل طلب:

```typescript
{
  // معلومات المستخدم
  user: {
    firstName: "صدام",
    username: "@saddamalwlai",
    telegramId: "7154440358"
  },
  
  // تفاصيل الطلب
  amount: 10000,                  // ✅ من Database
  method: "USDT",                 // ✅ من Database
  details: {                      // ✅ من Database
    walletAddress: "0x...",
    network: "TRC20"
  },
  
  // حالة
  status: "PENDING",              // ✅ من Database
  createdAt: "2024-01-15",
  processedAt: null
}
```

### 🔄 حالات الطلب:

```typescript
PENDING   🟡 → قيد المراجعة
APPROVED  🔵 → مُوافق عليه
COMPLETED 🟢 → مكتمل
REJECTED  🔴 → مرفوض
```

### ⚙️ الإجراءات المتاحة:

#### عندما الحالة = `PENDING`:

```
✅ موافقة     → يتحول إلى APPROVED
❌ رفض        → يتحول إلى REJECTED
👁️ عرض التفاصيل
```

#### عندما الحالة = `APPROVED`:

```
✅ تأكيد الإرسال → يتحول إلى COMPLETED
```

### 🔍 الفلتر:

```typescript
- جميع الطلبات
- قيد المراجعة (PENDING)
- مُوافق عليه (APPROVED)
- مكتمل (COMPLETED)
- مرفوض (REJECTED)
```

### 💡 سير العمل (Workflow):

```
1. المستخدم يطلب سحب → PENDING
2. الأدمن يراجع ويوافق → APPROVED
3. الأدمن يرسل المبلغ ويؤكد → COMPLETED

أو:
1. المستخدم يطلب سحب → PENDING
2. الأدمن يرفض الطلب → REJECTED
```

---

## 🔌 API Endpoints المستخدمة:

### 1. الإحصائيات الرئيسية:

```typescript
GET /api/admin/stats

Response:
{
  success: true,
  data: {
    totalUsers: 4,
    activeUsers: 2,
    totalTasks: 10,
    activeTasks: 5,
    completedTasks: 25,
    totalBalance: 42228,
    totalWithdrawals: 0,
    pendingWithdrawals: 0
  }
}
```

---

### 2. جلب جميع المستخدمين:

```typescript
GET /api/users/all

Response:
{
  success: true,
  data: [
    {
      id: "uuid",
      telegramId: "7154440358",
      username: "saddamalwlai",
      firstName: "صدام",
      balance: 36228,
      level: "BEGINNER",
      referralCount: 0,
      tasksCompleted: 0,
      status: "ACTIVE",
      createdAt: "2024-01-01",
      lastActiveAt: "2024-01-15"
    },
    // ...
  ],
  count: 4
}
```

---

### 3. جلب المهام:

```typescript
GET /api/tasks?limit=50

Response:
{
  success: true,
  data: {
    tasks: [
      {
        id: "uuid",
        name: "متابعة قناة تليجرام",
        description: "تابع قناتنا",
        reward: 5000,
        difficulty: "EASY",
        category: "SOCIAL_MEDIA",
        isActive: true,
        completionCount: 15,
        createdAt: "2024-01-01"
      },
      // ...
    ]
  }
}
```

---

### 4. تفعيل/تعطيل مهمة:

```typescript
PATCH /api/admin/tasks/[id]/toggle

Response:
{
  success: true,
  data: {
    id: "uuid",
    isActive: true  // تم التبديل
  }
}
```

---

### 5. جلب طلبات السحب:

```typescript
GET /api/withdrawals/all

Response:
{
  success: true,
  data: [
    {
      id: "uuid",
      userId: "uuid",
      user: {
        username: "saddamalwlai",
        firstName: "صدام",
        telegramId: "7154440358"
      },
      amount: 10000,
      method: "USDT",
      details: { walletAddress: "0x..." },
      status: "PENDING",
      createdAt: "2024-01-15",
      processedAt: null
    },
    // ...
  ],
  count: 0
}
```

---

### 6. تحديث حالة طلب سحب:

```typescript
PATCH /api/admin/withdrawals/[id]

Body:
{
  status: "APPROVED" | "REJECTED" | "COMPLETED"
}

Response:
{
  success: true,
  data: {
    id: "uuid",
    status: "APPROVED",
    processedAt: "2024-01-15T10:30:00Z"
  }
}
```

---

## 🎨 مميزات التصميم:

### 1. الألوان التفاعلية:

```typescript
// المستخدمون
Blue   → إجمالي المستخدمين
Green  → المستخدمون النشطون
Yellow → إجمالي الأرصدة
Purple → متوسط الرصيد

// المهام
Purple → إجمالي المهام
Green  → المهام النشطة
Blue   → مرات الإكمال
Yellow → المكافآت الموزعة

// طلبات السحب
Purple → إجمالي الطلبات
Yellow → قيد المراجعة
Green  → مكتمل
Red    → مرفوض
Blue   → إجمالي المبالغ
Orange → المبالغ المعلقة
```

### 2. الأيقونات:

```typescript
Users         → 👥 المستخدمون
Target        → 🎯 المهام
Send          → 📤 طلبات السحب
Coins         → 💰 الرصيد
TrendingUp    → 📈 الإحصائيات
CheckCircle   → ✅ نشط/موافق
Clock         → ⏱️ معلق
Eye           → 👁️ عرض
Edit          → ✏️ تعديل
Trash2        → 🗑️ حذف
```

### 3. التفاعلية:

```typescript
✅ hover effects على الأزرار
✅ transition animations
✅ scale effects على الكروت
✅ color coding للحالات
✅ responsive design
✅ loading states
✅ empty states
```

---

## 📱 الاستجابة (Responsive):

```typescript
// Desktop (lg)
- 4 أعمدة للإحصائيات
- 2 أعمدة للمهام
- عرض كامل للتفاصيل

// Tablet (md)
- 2-3 أعمدة للإحصائيات
- عمود واحد للمهام
- أزرار أصغر

// Mobile (sm)
- عمود واحد لكل شيء
- معلومات مختصرة
- أزرار مكدسة
```

---

## 🔒 الأمان (Security):

### ⚠️ الحالة الحالية:

```
❌ لا توجد حماية على الروابط
❌ أي شخص يمكنه الوصول إلى /admin
```

### ✅ الحماية الموصى بها:

```typescript
// 1. إضافة Middleware
// في middleware.ts
const ADMIN_IDS = ['7154440358'];

export function middleware(request: NextRequest) {
  if (request.nextUrl.pathname.startsWith('/admin')) {
    const telegramId = request.cookies.get('telegram_id')?.value;
    
    if (!telegramId || !ADMIN_IDS.includes(telegramId)) {
      return NextResponse.redirect(new URL('/mini-app', request.url));
    }
  }
  
  return NextResponse.next();
}
```

---

## 🚀 الاستخدام:

### 1. الوصول للوحة الأدمن:

```
https://alwalisoft-omega.vercel.app/admin
```

### 2. التنقل بين الصفحات:

```
لوحة التحكم → /admin
المستخدمون → /admin/users
المهام → /admin/tasks
طلبات السحب → /admin/withdrawals
```

### 3. إدارة المستخدمين:

```typescript
1. افتح /admin/users
2. ابحث عن مستخدم معين
3. استخدم الفلاتر (نشط، الرصيد، الإحالات)
4. انقر على "عرض" للتفاصيل
5. استخدم "تعديل" لتغيير البيانات
6. استخدم "حظر" لمنع المستخدم
```

### 4. إدارة المهام:

```typescript
1. افتح /admin/tasks
2. اختر الفلاتر (سهل، نشطة)
3. انقر على زر التفعيل/التعطيل لتغيير الحالة
4. استخدم "تعديل" لتغيير المهمة
5. استخدم "حذف" لحذف المهمة
6. اضغط "➕ مهمة جديدة" لإضافة مهمة
```

### 5. إدارة طلبات السحب:

```typescript
1. افتح /admin/withdrawals
2. راجع الطلبات المعلقة (PENDING)
3. اضغط "موافقة" للموافقة على الطلب
4. أو اضغط "رفض" لرفض الطلب
5. بعد إرسال المبلغ، اضغط "تأكيد الإرسال"
```

---

## 📊 البيانات الحقيقية vs التجريبية:

### ✅ بيانات حقيقية (من Database):

```typescript
✅ عدد المستخدمين
✅ رصيد كل مستخدم
✅ عدد المهام المنجزة
✅ عدد الإحالات
✅ حالة المهام (نشطة/معطلة)
✅ عدد مرات إكمال المهمة
✅ طلبات السحب وحالتها
✅ تواريخ الإنشاء والتحديث
```

### ❌ لا توجد بيانات تجريبية:

```
🎉 جميع البيانات حقيقية 100%!
```

---

## 🎯 الميزات المستقبلية:

```
🔜 إضافة مهام جديدة من الواجهة
🔜 تعديل بيانات المستخدمين
🔜 إرسال إشعارات جماعية
🔜 تحليلات وتقارير
🔜 تصدير البيانات (Excel/CSV)
🔜 فلاتر متقدمة
🔜 pagination للقوائم الطويلة
```

---

## 📝 الخلاصة:

```
✅ 4 صفحات أدمن كاملة
✅ بيانات حقيقية 100%
✅ واجهة سهلة الاستخدام
✅ تصميم احترافي
✅ responsive لجميع الأجهزة
✅ API endpoints جاهزة
✅ إحصائيات مباشرة
✅ إجراءات تفاعلية

⚠️ يحتاج: إضافة حماية (authentication)
```

---

**آخر تحديث:** 6 نوفمبر 2025 - 02:00  
**الحالة:** ✅ جاهز للاستخدام  
**البيانات:** 100% حقيقية من Database
