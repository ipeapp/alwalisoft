# 🤖 دليل إعادة تشغيل البوت

## ✅ البوت يعمل الآن بنجاح!

### 📋 الحالة الحالية:
- ✅ البوت متصل بـ Telegram API
- ✅ قاعدة البيانات تعمل بشكل صحيح
- ✅ جميع الـ handlers مفعّلة
- ⚠️ Redis غير متاح (لكن البوت يعمل بدونه)

---

## 🔧 إذا توقف البوت مستقبلاً:

### الطريقة السريعة (استخدم هذا الأمر):
```bash
cd /workspace && bash restart-bot.sh
```

### الطريقة اليدوية:
```bash
# 1. أوقف البوت
pkill -9 -f "bot/index"

# 2. أصلح صلاحيات قاعدة البيانات
bash fix-permissions.sh

# 3. شغّل البوت من جديد
rm -f bot.log
nohup pnpm dev:bot > bot.log 2>&1 &

# 4. تحقق من السجلات
sleep 5 && tail -30 bot.log
```

---

## 📊 الأوامر المفيدة:

### تحقق من حالة البوت:
```bash
ps aux | grep "bot/index" | grep -v grep
```

### اعرض آخر 50 سطر من السجل:
```bash
tail -50 bot.log
```

### اعرض السجل بشكل مباشر (Live):
```bash
tail -f bot.log
```

### تحقق من صلاحيات قاعدة البيانات:
```bash
ls -la prisma/dev.db
```

### اختبر اتصال البوت بـ Telegram:
```bash
curl -s https://api.telegram.org/bot8497278773:AAHSyGW3pcCGi3axsSXlaYRydLOqpUIcPoI/getMe
```

---

## ⚠️ المشاكل الشائعة وحلولها:

### 1. خطأ "readonly database"
**الحل:**
```bash
chmod 666 prisma/dev.db
chmod 777 prisma/
```

### 2. البوت لا يستجيب
**الحل:**
```bash
# أعد تشغيل البوت
bash restart-bot.sh

# تحقق من السجلات
tail -50 bot.log
```

### 3. خطأ "ETIMEDOUT" عند الاتصال بـ Telegram
**السبب:** مشكلة في الاتصال بالإنترنت  
**الحل:** انتظر قليلاً ثم أعد المحاولة

### 4. عمليات zombie كثيرة ([node] <defunct>)
**الحل:**
```bash
# أوقف جميع عمليات البوت
pkill -9 -f "bot/index"
pkill -9 -f "tsx"

# أعد التشغيل
bash restart-bot.sh
```

---

## 📱 اختبر البوت:

1. افتح تيليجرام
2. ابحث عن: **@makeittooeasy_bot**
3. اضغط على `/start`
4. يجب أن ترى:
   - رسالة ترحيب
   - زر "فتح التطبيق"
   - زر "المهام"
   - زر "الإحالات"

---

## 🔍 فحص صحة النظام:

```bash
# تحقق من جميع المكونات
cd /workspace && {
    echo "=== حالة البوت ==="
    ps aux | grep "bot/index" | grep -v grep && echo "✅ البوت يعمل" || echo "❌ البوت متوقف"
    
    echo ""
    echo "=== قاعدة البيانات ==="
    [ -f prisma/dev.db ] && echo "✅ قاعدة البيانات موجودة" || echo "❌ قاعدة البيانات مفقودة"
    [ -w prisma/dev.db ] && echo "✅ صلاحيات الكتابة صحيحة" || echo "❌ لا توجد صلاحيات كتابة"
    
    echo ""
    echo "=== الملفات المهمة ==="
    [ -f .env ] && echo "✅ ملف .env موجود" || echo "❌ ملف .env مفقود"
    [ -f bot/index.ts ] && echo "✅ كود البوت موجود" || echo "❌ كود البوت مفقود"
    
    echo ""
    echo "=== آخر 3 أسطر من السجل ==="
    tail -3 bot.log 2>/dev/null || echo "لا يوجد سجل"
}
```

---

## 🌐 التطبيق على Vercel:

الرابط: **https://alwalisoft.vercel.app**

### تحديث التطبيق:
```bash
# فقط ادفع التغييرات إلى GitHub
git add -A
git commit -m "your message"
git push origin main

# Vercel ستقوم بالنشر تلقائياً
```

---

## 💡 نصائح:

1. **احتفظ بالبوت يعمل دائماً:** استخدم `nohup` أو `pm2` للإنتاج
2. **راقب السجلات:** افحص `bot.log` بانتظام
3. **النسخ الاحتياطية:** احتفظ بنسخة من `prisma/dev.db`
4. **التحديثات:** اسحب آخر التحديثات من GitHub بانتظام

---

## 📞 الدعم:

إذا واجهت مشكلة لا تستطيع حلها:
1. تحقق من `bot.log` للأخطاء
2. تأكد من أن التوكن صحيح في `.env`
3. تحقق من صلاحيات قاعدة البيانات
4. أعد تشغيل البوت باستخدام `restart-bot.sh`

---

**آخر تحديث:** نوفمبر 2025  
**الحالة:** ✅ البوت يعمل بنجاح
