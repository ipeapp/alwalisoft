# 📱 التحليل الشامل للتطبيق - كل التفاصيل

**تاريخ:** 7 نوفمبر 2025  
**النوع:** تحليل فني شامل لكل جزء من التطبيق  
**الحالة:** قيد التطوير النشط

---

## 📊 نظرة عامة على البنية

### إحصائيات المشروع:

```
📦 المكونات الرئيسية:
├── 🌐 Frontend (Mini App):  17 صفحة    (4,936 سطر كود)
├── 🔧 Backend (API):          26 endpoint (2,633 سطر كود)
├── 🤖 Telegram Bot:           11 handler  (~1,500 سطر كود)
├── 🗄️ Database (Prisma):      37 model/enum
└── 📚 Libraries:              11 utility  (~800 سطر كود)

المجموع: ~9,900 سطر كود TypeScript/TSX
```

---

## 🎨 Frontend - Mini App (17 صفحة)

### 1. **الصفحة الرئيسية** (`app/mini-app/page.tsx` - 280 سطر)

**الوظيفة:**
- Dashboard رئيسي يعرض ملخص المستخدم
- عرض الرصيد، المهام المكتملة، عدد الإحالات
- Quick actions للميزات الرئيسية

**التقنيات:**
- `useAuth` hook للمصادقة
- `useEffect` لجلب البيانات من API
- Telegram WebApp SDK للتكامل

**البيانات المعروضة:**
- `balance` - الرصيد الحالي
- `tasksCompleted` - عدد المهام المكتملة
- `referrals` - عدد الإحالات
- `level` - مستوى المستخدم

**API Calls:**
```typescript
GET /api/users?telegramId=${telegramId}
```

**الحالة:** ✅ **يعمل بشكل كامل** - يجلب بيانات حقيقية

**المشاكل المحتملة:**
- ⚠️ `localStorage` access قد يفشل في SSR
- ⚠️ Redirect loop إذا فشل auto-login

---

### 2. **تسجيل الدخول** (`app/mini-app/login/page.tsx` - 255 سطر)

**الوظيفة:**
- Auto-login من Telegram WebApp
- Manual login بـ Telegram ID
- إنشاء حساب جديد

**آلية العمل:**
1. يتحقق من `Telegram.WebApp.initDataUnsafe.user`
2. إذا وُجد، يسجل دخول تلقائي
3. إذا لم يُوجد، يعرض نموذج الدخول اليدوي

**API Calls:**
```typescript
GET /api/users?telegramId=${telegramId}
POST /api/users (إذا لم يكن موجوداً)
```

**الحالة:** ✅ **يعمل** - تم إصلاح auto-login

**المشاكل:**
- ❌ لا يوجد validation قوي على Telegram ID
- ❌ لا يوجد CAPTCHA ضد البوتات

---

### 3. **المهام** (`app/mini-app/tasks/page.tsx` - 201 سطر)

**الوظيفة:**
- عرض جميع المهام النشطة
- تصنيف حسب الصعوبة والفئة
- إكمال المهام والحصول على المكافآت

**تصنيفات المهام:**
- `CHANNEL_SUBSCRIPTION` - الاشتراك في القناة
- `GROUP_JOIN` - الانضمام للمجموعة
- `CONTENT_SHARE` - مشاركة المحتوى
- `DAILY_LOGIN` - تسجيل الدخول اليومي
- `REFERRAL_BONUS` - مكافأة الإحالة

**API Calls:**
```typescript
GET /api/tasks?active=true&limit=20&userId=${userId}
POST /api/tasks/${taskId}/complete
```

**الحالة:** ✅ **يعمل** - يجلب بيانات حقيقية

**المشاكل:**
- ⚠️ لا يوجد verification حقيقي للمهام
- ⚠️ يمكن إكمال المهمة بدون التحقق الفعلي

---

### 4. **الإحالات** (`app/mini-app/referrals/page.tsx` - 255 سطر)

**الوظيفة:**
- عرض رابط الإحالة الشخصي
- إحصائيات الإحالات (3 مستويات)
- قائمة بالأصدقاء المُحالين

**نظام الإحالات:**
- **المستوى 1:** 500 عملة لكل إحالة مباشرة
- **المستوى 2:** 250 عملة (إحالات الإحالات)
- **المستوى 3:** 125 عملة (مستوى ثالث)

**API Calls:**
```typescript
GET /api/referrals?userId=${telegramId}
```

**الحالة:** ✅ **يعمل** - تم إصلاح الإحصائيات

**المشاكل:**
- ❌ لا يوجد حماية ضد الإحالات الوهمية
- ⚠️ `NEXT_PUBLIC_BOT_USERNAME` قد يكون undefined

---

### 5. **المكافآت** (`app/mini-app/rewards/page.tsx` - 393 سطر)

**الوظيفة:**
- المكافأة اليومية
- سلسلة الأيام (Streak)
- تاريخ المكافآت

**نظام المكافأة:**
- **مكافأة أساسية:** 500 عملة
- **زيادة يومية:** +50 عملة لكل يوم streak
- **أقصى مكافأة:** 1000 عملة

**API Calls:**
```typescript
GET /api/rewards/daily
POST /api/rewards/daily
```

**الحالة:** ✅ **يعمل** - تم إعادة كتابة Logic

**المشاكل:**
- ⚠️ قد يتلاعب المستخدم بساعة الجهاز

---

### 6. **الألعاب** (`app/mini-app/games/page.tsx` - 219 سطر)

**الوظيفة:**
- **Lucky Wheel** - عجلة الحظ
- **Target Hit** - إصابة الهدف
- **Quiz Challenge** - أسئلة

**API Calls:**
```typescript
POST /api/games/lucky-wheel
POST /api/games/target-hit
POST /api/games/quiz
```

**الحالة:** ✅ **يعمل**

**المشاكل:**
- ❌ **"Plays Today" = 0** - hardcoded
- ❌ **"Best Reward" = 0** - hardcoded
- ⚠️ لا يوجد rate limiting قوي

---

### 7. **المحفظة** (`app/mini-app/wallet/page.tsx` - 351 سطر)

**الوظيفة:**
- عرض الرصيد المتاح
- إحصائيات (إجمالي الأرباح، المسحوبات)
- طلبات السحب الأخيرة

**API Calls:**
```typescript
GET /api/withdrawals
GET /api/users?telegramId=${telegramId}
```

**الحالة:** ⚠️ **يعمل جزئياً**

**المشاكل:**
- ❌ **`totalWithdrawn` = 0** - hardcoded
- ❌ **`thisWeekEarnings` = 0** - hardcoded
- ❌ لا يوجد modal للسحب فعلياً

---

### 8. **المعاملات** (`app/mini-app/transactions/page.tsx` - 299 سطر)

**الوظيفة:**
- سجل جميع المعاملات
- تصفية حسب النوع
- تفاصيل كل معاملة

**API Calls:**
```typescript
GET /api/transactions
```

**الحالة:** ✅ **يعمل**

---

### 9. **الإنجازات** (`app/mini-app/achievements/page.tsx` - 433 سطر)

**الوظيفة:**
- 14 إنجاز مختلف
- تتبع التقدم لكل إنجاز
- مكافآت عند الإنجاز

**الإنجازات:**
- مهام: أول مهمة، 10 مهام، 50 مهمة
- أرصدة: 1K، 10K، 100K عملة
- إحالات: 5، 20، 100 صديق
- نشاط: 7 أيام، 30 يوم streak
- ألعاب: لاعب، عبقري، محظوظ

**API Calls:**
```typescript
GET /api/users/stats?telegramId=${telegramId}
```

**الحالة:** ⚠️ **يعمل جزئياً**

**المشاكل:**
- ❌ **لا يوجد Achievement model في Prisma**
- ❌ **لا يتم حفظ الإنجازات في DB**
- ❌ البيانات hardcoded

---

### 10. **الإشعارات** (`app/mini-app/notifications/page.tsx` - 344 سطر)

**الوظيفة:**
- عرض الإشعارات
- وضع علامة مقروء
- حذف الإشعارات

**أنواع الإشعارات:**
- `TASK` - مهمة جديدة
- `REWARD` - مكافأة
- `REFERRAL` - صديق جديد
- `ACHIEVEMENT` - إنجاز
- `GAME` - لعبة جديدة
- `SYSTEM` - نظام

**API Calls:**
- ❌ **لا يوجد API!**

**الحالة:** ❌ **Placeholder**

**المشاكل:**
- ❌ **يستخدم `mockNotifications`**
- ❌ **لا يوجد Notification model في Prisma**
- ❌ **لا يوجد API endpoints**

---

### 11. **المساعدة** (`app/mini-app/help/page.tsx` - 256 سطر)

**الوظيفة:**
- FAQs (15 سؤال)
- تصنيفات: البداية، المكافآت، الألعاب، إلخ
- روابط الدعم

**الحالة:** ✅ **يعمل**

**المشاكل:**
- ❌ **دردشة مباشرة: "قريباً"**
- ⚠️ FAQs hardcoded (يمكن جعلها dynamic)

---

### 12. **البروفايل** (`app/mini-app/profile/page.tsx` - 359 سطر)

**الوظيفة:**
- معلومات المستخدم
- إحصائيات تفصيلية
- تاريخ الانضمام

**API Calls:**
```typescript
GET /api/users/stats?telegramId=${telegramId}
```

**الحالة:** ✅ **يعمل**

---

### 13. **Leaderboard** (`app/mini-app/leaderboard/page.tsx` - 298 سطر)

**الوظيفة:**
- ترتيب أفضل المستخدمين
- تصنيفات: الرصيد، المهام، الإحالات
- موقعك في الترتيب

**API Calls:**
```typescript
GET /api/leaderboard?type=${type}&limit=50
```

**الحالة:** ✅ **يعمل**

---

### 14. **الإعدادات** (`app/mini-app/settings/page.tsx` - 282 سطر)

**الوظيفة:**
- معلومات الحساب
- الإشعارات، الصوت، الثيم
- اللغة
- تسجيل الخروج

**الحالة:** ⚠️ **واجهة فقط**

**المشاكل:**
- ❌ الإعدادات لا تُحفظ في DB
- ❌ تبديل اللغة لا يعمل فعلياً
- ❌ Privacy & Security - فارغ

---

### 15-17. **الألعاب الفرعية**

- **Target Hit** (314 سطر) - ✅ يعمل
- **Quiz** (377 سطر) - ✅ يعمل
- **Lucky Wheel** - في صفحة الألعاب الرئيسية

**الحالة:** ✅ **تعمل جميعها**

---

## 🔧 Backend - API Endpoints (26 endpoint)

### User Management (3 endpoints)

#### 1. `GET /api/users` (265 سطر)
```typescript
// وظيفة: جلب معلومات مستخدم أو قائمة المستخدمين
// المعاملات: telegramId أو username
// الاستخدام: في كل مكان تقريباً
```
**الحالة:** ✅ يعمل

#### 2. `GET /api/users/stats` (115 سطر)
```typescript
// وظيفة: إحصائيات المستخدم التفصيلية
// البيانات: tasks, referrals, streak, earnings
```
**الحالة:** ✅ يعمل

#### 3. `GET /api/users/all` (53 سطر)
```typescript
// وظيفة: قائمة جميع المستخدمين (للأدمن)
```
**الحالة:** ✅ يعمل

---

### Tasks (3 endpoints)

#### 4. `GET /api/tasks` (155 سطر)
```typescript
// وظيفة: جلب المهام مع تصفية
// التصفية: active, category, difficulty, limit
```
**الحالة:** ✅ يعمل

#### 5. `POST /api/tasks/[id]/complete` (152 سطر)
```typescript
// وظيفة: إكمال مهمة وإضافة المكافأة
// المنطق: 
//   1. التحقق من المستخدم
//   2. التحقق من المهمة
//   3. التحقق من الإكمال السابق
//   4. إضافة المكافأة
//   5. تحديث الإحصائيات
```
**الحالة:** ✅ يعمل

**المشاكل:**
- ⚠️ لا يوجد verification حقيقي

#### 6. `GET /api/tasks/[id]` (167 سطر)
```typescript
// وظيفة: تفاصيل مهمة واحدة
```
**الحالة:** ✅ يعمل

---

### Rewards (2 endpoints)

#### 7. `GET /api/rewards/daily` (204 سطر)
```typescript
// وظيفة: التحقق من توفر المكافأة اليومية
// الإرجاع: canClaim, nextClaimTime, streakDays
```

#### 8. `POST /api/rewards/daily` (نفس الملف)
```typescript
// وظيفة: المطالبة بالمكافأة اليومية
// المنطق:
//   1. التحقق من آخر claim
//   2. حساب streak
//   3. حساب المكافأة (500 + streak * 50)
//   4. إضافة الرصيد
//   5. تحديث DailyBonus
```
**الحالة:** ✅ يعمل - تم إعادة كتابة كاملة

---

### Games (3 endpoints)

#### 9. `POST /api/games/lucky-wheel` (98 سطر)
```typescript
// المكافآت: 50, 100, 200, 500, 1000, 5000, 10000
// الاحتمالات: weighted random
```

#### 10. `POST /api/games/target-hit` (98 سطر)
```typescript
// المكافأة: بناءً على score (0-200)
```

#### 11. `POST /api/games/quiz` (100 سطر)
```typescript
// المكافأة: 40 لكل إجابة صحيحة
```

**الحالة:** ✅ **جميعها تعمل**

---

### Withdrawals (4 endpoints)

#### 12-15. Withdrawal Management
```typescript
POST /api/withdrawals       - طلب سحب جديد
GET  /api/withdrawals       - طلبات المستخدم
GET  /api/withdrawals/all   - جميع الطلبات (أدمن)
POST /api/withdrawals/[id]/approve - موافقة
POST /api/withdrawals/[id]/reject  - رفض
```
**الحالة:** ✅ **تعمل جميعها**

---

### Referrals

#### 16. `GET /api/referrals` (63 سطر)
```typescript
// وظيفة: إحصائيات وقائمة الإحالات
// البيانات: referralTree, referrals list
```
**الحالة:** ✅ يعمل

---

### Others

#### 17. `GET /api/leaderboard` (49 سطر)
```typescript
// أنواع: balance, tasks, referrals
```

#### 18. `GET /api/transactions` (43 سطر)
```typescript
// سجل RewardLedger
```

#### 19. `GET /api/stats` (115 سطر)
```typescript
// إحصائيات عامة للنظام
```

#### 20. `GET /api/health` (64 سطر)
```typescript
// صحة النظام والاتصال بـ DB
```

**الحالة:** ✅ **جميعها تعمل**

---

### Admin Endpoints (6 endpoints)

#### 21-26. Admin Management
```typescript
GET  /api/admin/analytics     - إحصائيات شاملة
GET  /api/admin/stats         - stats للداشبورد
POST /api/admin/tasks/create  - إنشاء مهمة
POST /api/admin/tasks/[id]/toggle - تفعيل/إلغاء
POST /api/admin/withdrawals/[id] - إدارة السحب
POST /api/admin/notifications/send - إرسال إشعار
```

**الحالة:** ✅ **معظمها يعمل**

**المشاكل:**
- ❌ `notifications/send` - لا يوجد Notification model

---

## 🗄️ Database - Prisma Schema (37 Model/Enum)

### Models الرئيسية:

1. **User** - المستخدمون (17 field)
2. **Task** - المهام (27 field)
3. **TaskCompletion** - إكمال المهام
4. **RewardLedger** - سجل المكافآت
5. **DailyBonus** - المكافآت اليومية
6. **Referral** - الإحالات
7. **ReferralTree** - شجرة الإحالات
8. **Withdrawal** - طلبات السحب
9. **UserStatistics** - إحصائيات المستخدم
10. **Admin** - المسؤولون

### Models الناقصة:

❌ **Achievement** - للإنجازات  
❌ **Notification** - للإشعارات  
❌ **GamePlay** - لتتبع اللعب اليومي  
❌ **UserSettings** - لإعدادات المستخدم

---

## 🤖 Telegram Bot (11 Handler)

### Handlers الموجودة:

1. **start.ts** - بدء البوت والتسجيل
2. **tasks.ts** - إدارة المهام
3. **earn.ts** - طرق الكسب
4. **games.ts** - الألعاب
5. **referrals.ts** - نظام الإحالات
6. **withdraw.ts** - طلبات السحب
7. **stats.ts** - الإحصائيات
8. **settings.ts** - الإعدادات
9. **support.ts** - الدعم
10. **cards.ts** - البطاقات
11. **notifications.ts** - الإشعارات

**الحالة:** ✅ **جميعها تعمل**

---

## 📚 Libraries & Utilities (11 ملف)

1. **auth.ts** - JWT وإنشاء referral codes
2. **auth-context.tsx** - React Context للمصادقة
3. **prisma.ts** - Prisma Client
4. **api-response.ts** - Response helpers
5. **api-helpers.ts** - Validation helpers
6. **error-handler.ts** - معالجة الأخطاء
7. **rate-limiter.ts** - Rate limiting (Redis)
8. **api-cache.ts** - Caching (Redis)
9. **redis.ts** - Redis client
10. **security-headers.ts** - Security headers
11. **utils.ts** - Utility functions

**الحالة:** ✅ **جميعها تعمل**

**ملاحظة:** Redis اختياري (fallback إلى in-memory)

---

## 🎨 UI Components

### Shadcn/ui Components:
- `Button` - أزرار مخصصة
- `Card` - بطاقات
- `Dialog` - نوافذ منبثقة (غير مستخدم حالياً)
- `Input` - حقول إدخال (غير مستخدم حالياً)

### Custom Components:
- `Navigation` - شريط التنقل السفلي
- `ProtectedRoute` - حماية الصفحات
- `ThemeProvider` - إدارة الثيم

**الحالة:** ✅ **تعمل**

**المشاكل:**
- ⚠️ بعض Shadcn components غير مستخدمة

---

## 📋 الملخص - ما يعمل وما لا يعمل

### ✅ **يعمل بشكل كامل (80%):**

**Frontend:**
- Dashboard الرئيسي ✅
- تسجيل الدخول (auto-login) ✅
- المهام (عرض وإكمال) ✅
- الإحالات ✅
- المكافآت اليومية ✅
- الألعاب (3 ألعاب) ✅
- Leaderboard ✅
- المعاملات ✅
- البروفايل ✅
- المساعدة ✅

**Backend:**
- جميع User APIs ✅
- جميع Task APIs ✅
- Rewards API ✅
- Games APIs ✅
- Withdrawals APIs ✅
- Referrals API ✅
- Admin APIs (معظمها) ✅

**Bot:**
- جميع الـ 11 handlers ✅

---

### ⚠️ **يعمل جزئياً (15%):**

1. **المحفظة:**
   - ✅ يعرض الرصيد
   - ❌ `totalWithdrawn` = 0
   - ❌ `thisWeekEarnings` = 0
   - ❌ لا modal للسحب

2. **الإنجازات:**
   - ✅ يعرض الإنجازات
   - ✅ يحسب التقدم
   - ❌ لا يُحفظ في DB
   - ❌ لا يُمنح reward تلقائياً

3. **الإعدادات:**
   - ✅ واجهة جميلة
   - ❌ الإعدادات لا تُحفظ
   - ❌ تبديل اللغة لا يعمل

4. **الألعاب:**
   - ✅ تعمل اللعب
   - ❌ "Plays Today" = 0
   - ❌ "Best Reward" = 0

---

### ❌ **لا يعمل - Placeholders (5%):**

1. **الإشعارات:**
   - ❌ يستخدم mockNotifications
   - ❌ لا Notification model
   - ❌ لا API endpoints

2. **دردشة مباشرة (Help):**
   - ❌ "قريباً"

3. **Admin Notifications:**
   - ❌ API موجود لكن لا model

---

## 🔧 التقنيات المستخدمة

### Frontend Stack:
```
⚛️ Next.js 15 (App Router)
⚛️ React 19
📘 TypeScript 5.1
🎨 Tailwind CSS 3
🧩 Shadcn/ui Components
🌐 Telegram Web App SDK
```

### Backend Stack:
```
🟢 Node.js 20+
⚡ Next.js API Routes (Serverless)
🗄️ Prisma ORM 5
🐘 PostgreSQL (Neon)
🔐 JWT Authentication
⏱️ Rate Limiting (Redis)
💾 Caching (Redis)
```

### Bot Stack:
```
🤖 Telegraf.js 4
📝 Pino Logger
🔄 Process Management (PM2)
```

### Dev Tools:
```
📦 pnpm (Package Manager)
🔨 tsx (TypeScript Execution)
🎨 PostCSS
🧹 ESLint (غير مفعّل)
```

---

## 🚨 الأخطاء والمشاكل المحتملة

### 1. **أخطاء Runtime:**

#### A. localStorage في SSR
```typescript
// ❌ خطر:
localStorage.getItem('user')

// ✅ الحل:
if (typeof window !== 'undefined') {
  localStorage.getItem('user')
}
```
**الحالة:** ✅ تم الإصلاح في معظم الأماكن

#### B. Telegram WebApp undefined
```typescript
// ❌ خطر:
window.Telegram.WebApp.showAlert()

// ✅ الحل:
if (typeof window !== 'undefined' && window.Telegram?.WebApp) {
  window.Telegram.WebApp.showAlert()
}
```
**الحالة:** ✅ تم الإصلاح

#### C. Environment Variables
```typescript
// ⚠️ خطر:
process.env.NEXT_PUBLIC_BOT_USERNAME
// قد يكون undefined

// ✅ الحل:
const botUsername = process.env.NEXT_PUBLIC_BOT_USERNAME || 'default_bot';
```
**الحالة:** ⚠️ بعض الأماكن تحتاج fallback

---

### 2. **أخطاء الأمان:**

#### A. لا Validation على المهام
```typescript
// ❌ المشكلة:
// يمكن للمستخدم إكمال أي مهمة بدون verification

// ✅ الحل المطلوب:
// - التحقق من الاشتراك في القناة عبر Bot API
// - التحقق من الانضمام للمجموعة
// - إضافة verification في /api/tasks/[id]/complete
```

#### B. لا Rate Limiting قوي
```typescript
// ⚠️ المشكلة:
// يمكن اللعب بشكل غير محدود إذا تم تجاوز client-side check

// ✅ الحل:
// إضافة GamePlay model لتتبع المحاولات اليومية
```

#### C. لا CAPTCHA
```typescript
// ❌ المشكلة:
// يمكن إنشاء حسابات بوتات بسهولة

// ✅ الحل:
// إضافة hCaptcha أو reCAPTCHA
```

---

### 3. **أخطاء الأداء:**

#### A. N+1 Queries
```typescript
// ❌ مثال في /api/stats:
for (const tree of referralTrees) {
  const user = await prisma.user.findUnique({
    where: { id: tree.userId }
  });
}

// ✅ الحل:
const userIds = referralTrees.map(t => t.userId);
const users = await prisma.user.findMany({
  where: { id: { in: userIds } }
});
```
**الحالة:** ⚠️ موجودة في عدة أماكن

#### B. لا Indexes على DB
```typescript
// ⚠️ المشكلة:
// Queries بطيئة بدون indexes

// ✅ الحل:
@@index([telegramId])
@@index([referralCode])
@@index([userId, createdAt])
```
**الحالة:** ⚠️ بعض الـ indexes مفقودة

#### C. لا Pagination
```typescript
// ⚠️ المشكلة:
// جلب جميع السجلات بدون limit

// ✅ الحل:
// إضافة pagination في جميع list APIs
```

---

### 4. **أخطاء UX:**

#### A. لا Loading States
```typescript
// ⚠️ بعض الصفحات لا تعرض loading
// ✅ تحتاج إلى Skeleton loaders
```

#### B. لا Error Boundaries
```typescript
// ❌ أي خطأ يكسر الصفحة كاملة
// ✅ إضافة Error Boundary components
```

#### C. لا Offline Mode
```typescript
// ❌ لا يعمل بدون إنترنت
// ✅ إضافة Service Worker + offline storage
```

---

## 📝 التوثيق الداخلي

### ✅ موجود:
- README.md ✅
- README_AR.md ✅
- ARCHITECTURE.md ✅
- ADMIN_GUIDE.md ✅
- التحليل الشامل (هذا الملف) ✅

### ❌ ناقص:
- API Documentation ❌
- Component Documentation ❌
- Deployment Guide للبوت ❌
- Troubleshooting Guide ❌

---

## 🎯 التقييم العام

```
┌─────────────────────────────────────┐
│  📊 درجة التطوير الإجمالية: 80%  │
├─────────────────────────────────────┤
│  ✅ Frontend:          85%         │
│  ✅ Backend API:       90%         │
│  ✅ Database:          85%         │
│  ✅ Bot:               95%         │
│  ⚠️ Security:          60%         │
│  ⚠️ Performance:       70%         │
│  ⚠️ Testing:           0%          │
│  ⚠️ Documentation:     65%         │
└─────────────────────────────────────┘
```

---

**هذا الملف يحتوي على:** تحليل شامل لكل صفحة، API، ومكون في التطبيق

**الملفات التالية:**
- `MISSING_FEATURES_AR.md` - الميزات الناقصة بالتفصيل
- `ADS_INTEGRATION_GUIDE_AR.md` - كيفية ربط الإعلانات
- `DEVELOPMENT_PLAN_AR.md` - خطة التطوير القادمة
